# ── Streamlit app runtime dependencies (Streamlit Community Cloud) ──
# lean set — เฉพาะที่ streamlit_app.py + dspy_module.py ใช้จริงตอน runtime
# (ไม่รวม torch/transformers/sentence-transformers/whisper เพื่อให้ build ผ่าน limit ของ Cloud)
streamlit>=1.58.0
dspy-ai>=2.4.0               # DSPy framework
litellm>=1.83.0             # LLM router (openrouter / typhoon / gemini)
httpx>=0.27.0               # async HTTP (YouTube API)
requests>=2.32.0
python-dotenv>=1.0.0
pymupdf>=1.24.0             # PDF text extraction (primary)
pdfminer.six>=20221105      # PDF text extraction (fallback)
gtts>=2.5.0                 # text-to-speech
youtube-transcript-api>=1.2.4
